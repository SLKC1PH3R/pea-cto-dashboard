DATABASE_URL="postgresql://user:password@localhost:5432/pea_cto_dashboard?schema=public"

# Finnhub API (https://finnhub.io) — plan gratuit suffisant pour les cotations
FINNHUB_API_KEY=""

# NextAuth — génère un secret avec: openssl rand -base64 32
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"
